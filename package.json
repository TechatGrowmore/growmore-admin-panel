{
  "name": "growmore-admin-panel",
  "version": "1.0.0",
  "description": "GrowMore Admin Panel - MERN Stack (single Render service)",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend --include=dev && npm run build --prefix frontend",
    "start": "node backend/server.js",
    "dev": "concurrently \"nodemon backend/server.js\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.3"
  }
}
